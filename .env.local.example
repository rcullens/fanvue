# Optional — without these, the local mock reply engine is used.
OPENAI_API_KEY=
# OpenAI-compatible base URL (default: https://api.openai.com/v1)
OPENAI_BASE_URL=
# Model id (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
